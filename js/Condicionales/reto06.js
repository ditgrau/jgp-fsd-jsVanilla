/* Pedir una cadena de texto y comprobar que todas sus letras estén en minúsculas. */

let string = prompt('Introduzca una frase');

if (string === string.toLowerCase) {
    alert (`Todas las letras estan en minúsculas`) 
}else {
    alert ('Hay letras en mayúsculas')
}