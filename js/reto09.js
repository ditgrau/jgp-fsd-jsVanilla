/* 9. Muestra los números del 1 al 100 (ambos incluidos) divisibles entre 2 y 3. Utiliza el bucle
que desees. */

for (let num = 2; num <= 200; num+=2) { 
    // si pongo num+2 no se esta asignando el valor a num, en cada iteracion num volveria a ser =0, += suma y asigna.
    console.log (num);
}   
for (let num = 3; num <= 300; num+=3) { 
    console.log (num);
}   

